// Escreva uma função TypeScript chamada multiplicar que aceita dois números 
// como argumentos e retorna o produto deles

function produto (a:number,b:number) {
    return (a * b)
}

console.log(produto(10,2))